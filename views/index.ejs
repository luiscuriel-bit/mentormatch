<!DOCTYPE html>
<html lang="en">

<%- include("./partials/head.ejs") %>

    <body>
        <% if (user) { %>
            <h1>Welcome to MentorMatch, <%= user.username %>!</h1>
            <p>
                <a href="/auth/sign-out">Sign out</a>
            </p>
            <% } else { %>
                <h1>Welcome to the app, guest.</h1>
                <p>
                    <a href="/auth/sign-up">Sign up</a> or <a href="/auth/sign-in">Sign in</a>.
                </p>
            <% } %>
    </body>

</html>